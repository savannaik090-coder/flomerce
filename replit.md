# Flomerce SaaS Platform

## Overview
Flomerce is a multi-tenant SaaS platform designed for small businesses and entrepreneurs to create scalable e-commerce websites from templates, each hosted on a unique subdomain (e.g., `store-name.flomerce.com`). It provides efficient online storefronts, comprehensive sales capabilities, and brand-building tools through a Cloudflare-centric serverless architecture. The platform aims to be an accessible and powerful solution for establishing an online presence. Key features include dynamic content, subscription management, an admin panel with an iframe preview, product options, subcategories, multi-currency support, a robust order flow with cancellation and return systems, product reviews, inventory locations, storage usage tracking, admin shard management, built-in analytics, and global shipping configurations.

## User Preferences
I prefer iterative development with clear communication on significant changes. Please ask before making major architectural decisions or large-scale code overhauls. Provide concise explanations and focus on effective solutions. Do not make changes to files in the `frontend/templates/` folder.

## Build Instructions (IMPORTANT)
**After every frontend code change, you MUST run the build scripts before finishing:**
- Storefront: `cd frontend/src/storefront && npm run build`
- Platform: `cd frontend/src/platform && npm run build`
Both builds output to the `frontend/` directory (storefront to `frontend/storefront/`, platform to `frontend/`). Always run both builds after any change to files in `frontend/src/`.

## System Architecture

### Core Design
Flomerce utilizes a shared shard-based D1 database architecture where multiple sites share Cloudflare D1 databases (shards) with `site_id`-based row isolation. A platform database (`env.DB`) stores global data (users, site metadata, subscriptions), while site-specific data (products, orders) resides in shard D1 databases, always filtered by `site_id`.

### Shared Shard Architecture
- **Shards Registry:** `shards` table in the platform DB tracks all shard databases.
- **Site Assignment:** New sites are assigned to an active shard.
- **Binding Pattern:** Shards are dynamically bound to workers.
- **DB Resolution:** Utilities resolve the correct shard DB.
- **Subscription Management:** Plan names are preserved on expiry, and subscription priority ensures correct trial reporting. Enterprise plan safeguards prevent accidental downgrades.

### Usage Tracking
- **Row-Level Tracking:** Estimated `row_size_bytes` contributes to `site_usage.d1_bytes_used`.
- **Correction Factor:** A per-shard `correction_factor` reconciles estimated usage with actual DB size.
- **R2 Tracking:** Managed via the `site_media` table.
- **Plan Limits:** Hard limits for Basic/Standard/Pro plans, with enterprise plans allowing overage.

### Plan-Based Feature Gating
- **Plan hierarchy:** Trial (full Pro access, 5 sites max, 7-day expiry) → Starter → Growth → Pro → Enterprise.
- **Count limits:** Sites (trial: 5), Staff (starter: 5, growth: 25), Inventory locations (starter: 2, growth: 50). Enforced in `sites-worker.js`, `site-admin-worker.js`, `inventory-locations-worker.js`.
- **Feature flags:** Reviews, Blog, Advanced SEO (incl. per-page/category/product SEO), and Coupons are available from Starter onwards (matches Shopify/Wix/Dukaan industry norms). Push Notifications (manual), Revenue analytics, Appointment Booking, and Remove "Powered by Flomerce" branding require Growth+. Push Notifications (automated) requires Pro+.
- **Backend enforcement:** `checkFeatureAccess(env, siteId, feature)` and `checkCountLimit(env, siteId, limitType)` in `backend/utils/usage-tracker.js`. Feature checks use `request.clone().json()` fallback to extract `siteId` from body when query param is missing. `GET /api/plan-limits` endpoint returns all limits for a site (incl. `appointmentBooking`, `removeBranding`). Storefront appointment endpoint (`POST /api/email/appointment`) gates on `appointmentBooking` when `siteId` is provided in the body. Removing branding is enforced storefront-side: the Footer renders "Powered by Flomerce" unless `settings.footer.hideBranding === true` AND `siteConfig.subscriptionPlan` is Growth+, so a downgraded merchant automatically loses the toggle's effect.
- **Frontend gating:** `FeatureGate` overlay component (`frontend/src/storefront/src/components/admin/FeatureGate.jsx`) wraps locked sections with upgrade prompt; normalization helpers are extracted to `frontend/src/storefront/src/utils/plan.js` (`normalizePlan`, `isPlanAtLeast`) so storefront components share the same plan logic as the admin. `AdminSidebar` shows lock icons and plan badges. `VisualCustomizer` gates the Book Appointment section (Growth+); Blog, Customer Reviews, and SEO subtabs are unlocked from Starter. The Footer editor (Edit Website → Footer tab) has a "Branding" card with a `Hide "Powered by Flomerce"` toggle gated at Growth+. Storefront `Navbar`, `NavbarModern`, `StoreLocations`, and `BookAppointmentPage` hide/redirect the appointment booking flow when the site plan is below Growth. `DashboardPage` enforces trial site creation limit and staff count limits with disabled buttons.
- **Trial expiry:** Enforced just-in-time by `reconcileSiteSubscription()` in `backend/workers/platform/sites-worker.js`, called on every site fetch. Sites with `subscription_plan='trial'` and a past `subscription_expires_at` are cleared (plan and expiry reset to NULL) and treated as `free` until the user upgrades. Active subscriptions and the `enterprise` admin override are preserved.
- **`subscription_plan`** is included in site info response and mapped as `siteConfig.subscriptionPlan` in `SiteContext.jsx`.

### Admin Shard Management API
- Provides endpoints for managing shards (listing, creating, reconciling, activating, moving, deleting).

### Translation Architecture (System A landing-only + System B storefront — as of Apr 24, 2026)
- **System A (platform i18next) is restored ONLY for the public landing surfaces.** The components that load `react-i18next` and call `useTranslation()` are exactly: `LandingPage.jsx`, `Navbar.jsx` (with `LanguageSwitcher`), `LandingPricing.jsx`, `ContactForm.jsx`, and `AboutPage.jsx` — five files. The owner Translations admin tab (`I18nAdminPanel.jsx`) is rendered with hard-coded English labels — the panel still controls the same backend translation worker (regenerate / refresh / purge / per-key edits for non-English locales), but the panel chrome itself is English. Everything else in the platform SPA — dashboard, site-creation wizard, owner admin (other tabs), auth pages (login/signup/verify-email/reset-password), and legal pages (terms/privacy/refund/shipping) — stays hard-coded English. The storefront SPA loads zero `i18next` packages; it uses System B exclusively.
- **Sibling-component pattern for shared chrome.** `Navbar` is the only piece of chrome reused outside the landing surface, so it has a translated and a non-translated sibling:
  - `Navbar.jsx` (translated, with `LanguageSwitcher`) is imported by `LandingPage.jsx` and `AboutPage.jsx`.
  - `LegalNavbar.jsx` (hard-coded English chrome but **does** host the shared `LanguageSwitcher` so visitors can switch language from a legal page and have it persist) is imported by `TermsPage.jsx`, `PrivacyPolicyPage.jsx`, `RefundPolicyPage.jsx`, and `ShippingPolicyPage.jsx`. The legal page bodies themselves stay hard-coded English — only the language dropdown crosses into this file. (`AboutPage.jsx` used to import LegalNavbar but was promoted to System A on Apr 24, 2026 and now imports the translated `Navbar.jsx`.)
  - `LegalBindingBanner.jsx` (one small banner that says "English version is binding…") and `LegalFooter.jsx` (the footer with the 5 page links + address line + copyright) are the only other sanctioned `useTranslation` consumers that appear on legal pages — both follow the same pattern: a small self-contained component owning its own `t()` calls so the page body itself can stay hard-coded English. Their keys are: banner → `common.legalBindingTitle` + `common.legalBindingBody`; footer → `landing.footerAbout` / `footerTerms` / `footerPrivacy` / `footerRefund` / `footerShipping` / `footerAddress` / `rightsReserved`. The footer reuses the `landing` namespace (rather than duplicating into a `legal` namespace) so all six footer keys translate identically across LandingPage, AboutPage, and the four legal pages — one source of truth per phrase. `phone` + `email` are passed in as props so the universal contact strings (numbers, addresses) stay raw English even in non-English locales. Do **not** add any further `useTranslation()` / `t()` calls in the legal page bodies — if you ever need another translated meta-element on a legal page, follow the same pattern.
- **System A.5 — `/api/i18n/plans/:lang` for owner-defined dynamic plan strings (Apr 24, 2026).** The static landing strings (Pricing / Get Started / duration buttons / etc.) translate via the regular i18next catalog, but the owner-defined plan content — `plan_name`, `tagline`, `features[]` per `subscription_plans` row, plus `enterprise_message` from `platform_settings` — does NOT live in any JSON catalog because it changes at runtime. A separate worker route `GET /api/i18n/plans/:lang` returns a per-language overlay that `LandingPricing.jsx` merges on top of the raw `/api/payments/plans` response (`applyPlanTranslations` keys by `plan_name`). **Cost shape (4-tier hot path, ordered):** (1) CDN edge HIT — zero worker invocation, max-age 7d; (2) CDN edge MISS, R2 HIT — single R2 read, NO D1 query (verified: handlePlansLocale only loads EN from D1 on the cold/fallback paths via the `loadEN` memo); (3) R2 MISS — one D1 read of EN payload + one Microsoft Translator batch + write-through to R2; (4) translator failure or per-locale daily rate-limit hit — falls back to EN payload with `transient` cache header so it doesn't get stuck at the edge. **Auto-purge contract (load-bearing):** every code path that writes to `subscription_plans` OR writes `enterprise_message` MUST call `await firePlansPurge(env)` after the DB write — currently that means `createPlan`, `updatePlan`, `bulkSavePlan`, `bulkDeletePlan`, `deletePlan`, `updateSettings` (only when the `enterprise_message` key is in the payload — gated by the `plansOverlayDirty` flag), AND the opportunistic deprecated-cycle cleanup inside `getPlans` (only when it actually deletes rows). The purge is **awaited, not fire-and-forget** — Cloudflare Workers drop un-awaited Promises that aren't wrapped in `ctx.waitUntil`, which would silently break the contract under load; the ~100-300ms latency cost is acceptable on admin-only mutation paths. Internally `firePlansPurge` try/catches the CF API call so a network blip never converts a successful save into a user-facing error; stale data self-heals at the 7d TTL in the worst case. The purge itself fires R2 deletes + a Cloudflare URL purge for every language ever lazy-generated (discovered via R2 prefix listing of `platform-plans/`). If the R2 list call itself fails (transient blip / throttle), the purge falls back to the full `SUPPORTED_LOCALES` set so no stale edge entry survives a successful admin save — the R2 list is the optimization (smaller purge surface, saves CF API quota), the SUPPORTED_LOCALES fallback is the correctness net. Why this is a separate endpoint instead of baking plan strings into the locale catalog: plan edits must go live AUTOMATICALLY (no "Refresh all out-of-date" click) — owners edit plans far more often than static UI copy, and forcing them through the manual refresh workflow for a price change would be a footgun. The trade-off: this duplicates the System A invariant in a smaller form (locale catalog → manual purge via admin "refresh"; plans overlay → automatic purge on save). Do NOT merge the two pipelines without re-establishing both invariants. **Frontend contract:** `paymentService.js` exports `getPlanTranslations(lang)`; `LandingPricing.jsx` re-runs `loadPlans` whenever `i18n.language` changes; on translation fetch failure the component falls back silently to English plan strings (the rest of the UI stays in the visitor's language); the canonical English `plan_name` is preserved as `_original_plan_name` on every overlaid plan so any future code path that needs the raw key (analytics, Razorpay flow, server lookups) can still find it. Files: backend → `i18n-worker.js#handlePlansLocale` + `cdn-cache.js#purgePlansLocaleCache` + `admin-worker.js#firePlansPurge`; frontend → `paymentService.js#getPlanTranslations` + `LandingPricing.jsx#applyPlanTranslations`.
  - `DashboardPlanSelector.jsx` (hard-coded English, no `useTranslation`) is the dashboard's plan-management UI, imported only by `DashboardPage.jsx`. There is no translated sibling — pricing UI on the landing page is rendered inline by `LandingPricing.jsx` itself, not by `DashboardPlanSelector`.
  - **Do not consolidate these back into one shared component** — the duplication is the contract that keeps i18next from leaking onto pages that must stay English-only. Future "DRY refactors" that merge `Navbar` + `LegalNavbar` will silently re-introduce the scope leak.
- **Vite dedupe is critical.** `frontend/src/platform/vite.config.js` deduplicates and aliases `i18next`, `react-i18next`, and `i18next-browser-languagedetector` to `frontend/src/platform/node_modules/*`. Without this, the bundler resolves `init.js`'s `import 'i18next'` to one instance and `react-i18next`'s internal `import 'i18next'` to another, so `initReactI18next` lands on instance A while `useTranslation` reads instance B and returns raw keys. The root `package.json` therefore must NOT list `i18next` / `react-i18next` / `i18next-browser-languagedetector` — they belong only to `frontend/src/platform/package.json`.
- **Restored files:** `frontend/src/shared/i18n/` (16 EN namespace JSONs + `init.js` + `index.js` + `LanguageSwitcher.jsx`); `frontend/src/platform/src/components/I18nAdminPanel.jsx`; `backend/workers/platform/i18n-worker.js`. Routes re-wired in `backend/workers/index.js` (`case 'i18n': handleI18nPublic`) and `backend/workers/platform/admin-worker.js` (`case 'i18n': handleI18nAdmin`). The owner admin page mounts the panel under a Translations tab (`activeTab === 'i18n'`).
- **Per-merchant on-demand storefront translation — hybrid server + client model (Apr 2026 refactor).** There are now TWO translation paths in the storefront, both backed by the same merchant Microsoft Translator key (encrypted on `sites.translator_api_key_encrypted` + `translator_region` + `translator_languages`) and the same per-site `translation_cache` shard table:
  - **Server-composed payloads (DB + CDN, primary path).** Storefront API endpoints (`/api/products`, `/api/products/:id`, `/api/categories`, `/api/categories/:id`, `/api/blog/posts`, `/api/blog/post/:slug`, `/api/reviews/product/:id`, `/api/site`) accept a `?lang=` query parameter, translate the merchant-authored content fields server-side via `backend/utils/server-translator.js#translateContentBatch`, and emit `Cache-Control` + `CDN-Cache-Control` headers so each `?lang=` variant lives at the Cloudflare edge for 7 days. Frontend `apiRequest` (`frontend/src/storefront/src/services/api.js`) auto-appends `&lang=` from `localStorage.flomerce_lang` to every storefront GET (skipping mutations, admin contexts, and the `/storefront/translate` proxy itself); `SiteContext.fetchSiteConfig` does the same inline because it bootstraps before `apiRequest` is on the path. When the shopper switches language via `LanguageSwitcher`, `App.jsx` listens for the `flomerce_lang_change` event and triggers a hard reload so every component refetches in the new language. SEO is served the same way: `applySEO` (`backend/workers/seo/index.js`) reads `?lang=`, translates `<title>` / meta description / OG / Twitter / JSON-LD via the same helper, sets `<html lang>` + `og:locale`, emits `<link rel="alternate" hreflang>` for every enabled language + `x-default`, and rewrites the canonical URL with `?lang=` for non-default variants. `generateSitemap` emits `<xhtml:link>` alternates per URL. Per-language cache purge: `backend/utils/cache.js#runPurgeNow` reads `translator_languages` off the site row and fans out one `?lang=X` purge URL per enabled language for every base URL, so a merchant edit invalidates every cached language variant within the existing 30s debounce window.
  - **Default-content wrapping pass (Apr 25, 2026 follow-up).** The bundle architecture below loads pre-translated strings into a runtime dict, but components only translate text they explicitly wrap in `<TranslatedText>`. Rewrapped: `home/HeroSlider.jsx` (slide title/subtitle/description/buttonText), `home/FeaturedVideoSection.jsx` (title/description/chatButtonText), `home/CustomerReviews.jsx` (sectionTitle/subtitle + default review text/name when settings.reviews is empty), `home/ProductShowcase.jsx` (showcaseTitle, default product names, popup, "Add Set to Bag", "Add to Cart", "View Details"), `home/FirstVisitBanner.jsx` (heading/message/buttonText with brand-name interpolation split out), `home/ChooseByCategory.jsx` (cat.name). Also added `CategorySection.jsx` (category.name/subtitle + banner title), `SubcategorySection.jsx` (removed `.toLowerCase()` before lookup so dict hits work), `StoreLocations.jsx` (store.name/address/hours, with the inline string defaults moved to `defaults/generic.js#storeLocationDefaults` so the manifest scanner picks them up), `ShopTheLook.jsx` (product.name in list + popup). Extended `scripts/extract-i18n.cjs` field regex to also catch `hours|address|brandedNameSuffix|intro|note|hint|prompt|warning|error|reason|reply`. Also fixed the promo banner translation gap in `backend/workers/index.js#walkTranslatableLeaves` — the walker recursed into arrays but only translated string properties of OBJECTS, never plain-string array elements, so `settings.promoBanner = ['msg1', 'msg2']` was silently skipped. Walker now inherits the parent key when entering an array and translates string elements in-place when the parent key is translatable; added `messages|banner|banners|note|notes|hours|address` to the suffix list. Wrapped Navbar's hardcoded "Welcome to {brand}" fallback while leaving `brandName` itself as a literal. Removed `brand_name` from `translateSiteInfoInPlace` — brand names are proper nouns and short tokens routinely mangled by machine translation, so the original spelling is preserved across all languages. **Templated phrase support:** extended `TranslatedText` with a `vars` prop that substitutes `{{key}}` tokens AFTER lookup, so phrases with dynamic literals (brand names, numbers) translate as ONE unit and preserve target-language word order — e.g. `<TranslatedText text="Welcome to {{brand}}" vars={{ brand: 'XXJ' }} />` renders `XXJ में आपका स्वागत है` in Hindi (brand BEFORE the verb), not the broken `में आपका स्वागत है XXJ`. The double-brace syntax matches i18next placeholder convention so the existing `protectPlaceholders` round-trip in `backend/utils/translator.js` wraps `{{brand}}` in `<span class="notranslate">` and Microsoft leaves the token untouched. Applied in Navbar promo banner fallback and FirstVisitBanner heading. Manifest re-extracted (451 strings, hash `b986630c671d20c4`). **Inline-fallback audit (Apr 25, 2026 follow-up #2).** The extractor in `scripts/extract-i18n.cjs` only sees literals when they're the direct `text=` attribute value of a `<TranslatedText>` element — it cannot discover strings hidden behind `||` fallbacks like `const x = settings.foo || "Literal"; ... <TranslatedText text={x} />`. Audited every storefront component and converted every such inline fallback into a ternary that uses TWO separate `<TranslatedText>` elements: `{settings.foo ? <TranslatedText text={settings.foo} /> : <TranslatedText text="Literal" />}`. Both branches now have a literal `text="..."` that the regex catches, so the merchant-typed value AND the default both flow through the bundle dict. Files touched: `home/CustomerReviews.jsx`, `templates/modern/CustomerReviewsModern.jsx`, `home/ProductShowcase.jsx`, `templates/modern/BrandStory.jsx`, `home/FirstVisitBanner.jsx`, `templates/modern/HeroSplit.jsx`, `home/HeroSlider.jsx`, `home/WatchAndBuy.jsx`, `home/FeaturedVideoSection.jsx`. New fallback literals added to manifest: "What Our Customers Say", "Real reviews from our happy customers", "Lumina Collection", "Our Story", the long brand-story body copy, "Discover our exquisite collection. Sign up today to receive exclusive offers and updates.", "Sign Up Now", "New Collection", "Welcome", "Discover our curated collection of premium products.", "Shop Now", "SHOP NOW", "Video", "View Product", "CHAT NOW". Manifest now 464 strings, hash `db74c243ed354cc5`. **Raw `{product.name}` audit (Apr 25, 2026 follow-up #3).** Even though "Premium Product" / "Best Seller" / etc. were extracted into the bundle dict at hash `db74c243ed354cc5`, the storefront was rendering them as `{product.name}` (raw JSX expression, no `<TranslatedText>` wrapper) — so the dict lookup never ran and the shopper saw English. The product cards/details receive `name` either from `/api/products` (already server-translated for that target lang) OR from `defaults/generic.js#trendingProductsDefaults` (English literals, never touched by server translator). Wrapping in `<TranslatedText text={product.name} />` handles both cases: real products' already-translated names hit a dict miss and fall through to the input value (no harm), while default-product English names hit the bundle dict and resolve to Hindi/etc. Files touched: `components/product/ProductCard.jsx` (name + "Out of Stock" badge), `pages/ProductDetailPage.jsx` (title h1 + description body + "Out of Stock"/"In Stock" badge + "OUT OF STOCK"/"ADD TO CART" CTA + "REMOVE FROM WISHLIST"/"ADD TO WISHLIST" CTA), `components/layout/SearchOverlay.jsx` (search-result name + category_name). Manifest now 468 strings, hash `12ff59b0b62cb5b4`. **Brand-templated showroom + placeholder-spacing fix (Apr 25, 2026 follow-up #4).** Two related bugs surfaced when the test merchant (brand "Djd") used the default StoreLocations card and the welcome banner: (a) `StoreLocations.jsx` was building the card title via JS string concatenation `${siteConfig.brandName} ${storeLocationDefaults.brandedNameSuffix}` → literal "Djd Showroom" — that exact string was never in the bundle (every brand creates a unique combination), so the dict lookup missed and the card stayed English. (b) Microsoft Translator collapsed the whitespace adjacent to the notranslate-span placeholder when translating "Welcome to {{brand}}!" → cached `"{{brand}}में आपका स्वागत है !"` (no space between `{{brand}}` and `में`), so after substitution the user saw "Djdमें आपका स्वागत है!" jammed together. Fixed by: (a) adding `storeLocationBrandedTemplate = { name: '{{brand}} Showroom' }` constant to `defaults/generic.js` so the i18n extractor catches the full template phrase as one translation unit, then `StoreLocations.jsx` uses that template + passes `_nameVars: { brand }` through to `<TranslatedText text={store.name} vars={store._nameVars} />` so the brand stays in original spelling. (b) Hardening `TranslatedText.jsx` substitution: when replacing `{{key}}` placeholders, defensively re-insert a single space whenever the placeholder boundary touches a Unicode letter (`\p{L}`) on either side. The substitution now temporarily marks the placeholder site, runs two regex replacements (`(\p{L})MARKER` → `\p{L} MARKER` and `MARKER(\p{L})` → `MARKER \p{L}`), then drops in the literal value — so even with the broken cached translation the rendered output is "Djd में आपका स्वागत है !". The padding only fires for letter-adjacent collisions, so "{{brand}}!" stays "Djd!" (punctuation untouched). Manifest now 470 strings, hash `1a2a4816ff8b4bc1`.
  - **`<TranslatedText>` pre-translated bundle (residual chrome path — Apr 25, 2026 rewrite).** Every static literal (button labels, modal copy, empty-state messages, ~440 hard-coded strings) AND every default-content placeholder string (sample products, demo reviews, hero fallbacks shipped in `frontend/src/storefront/src/defaults/*.js`) is now served from a SINGLE pre-translated dictionary by `GET /api/storefront/:siteId/translations/:lang?v=<bundleVersion>` (handler: `backend/workers/storefront/translations-bundle.js`). The dict is keyed by `djb2(source) → translated` (must match `clientHash` in `ShopperTranslationContext.jsx` byte-for-byte — same DJB2, base36). The dictionary contents are extracted at build time by `scripts/extract-i18n.cjs` (regex-scans every `<TranslatedText text="..."/>` literal + every translatable field in `defaults/*.js` → writes `backend/i18n-manifest.json` with sorted strings + sha256-truncated `hash`). The bundle version embedded in the URL is `manifestHash + '.' + (translator_enabled ? '1' : '0')` — bumped automatically on every code deploy that touches a literal AND on every merchant translator-toggle flip, so the Cloudflare edge auto-purges with zero manual cache invalidation. Headers: `Cache-Control: public, max-age=300, s-maxage=2592000` + `CDN-Cache-Control: public, s-maxage=31536000, immutable` + `Cloudflare-CDN-Cache-Control` (mirror, for some Cloudflare configs). On cold cache the endpoint pumps the manifest through `translateContentBatch` (D1-cached per `(site, target_lang, hash)`); on a warm D1 cache the response is sub-100ms with zero MS API calls. **Critical:** the bundle endpoint passes `{ bypassDailyCap: true }` to `translateContentBatch` because chrome+defaults warming is one-time per `(site, lang)` and forever D1-cached after — it MUST NOT consume the merchant's per-day product-translation budget (`TRANSLATOR_DAILY_CAP`, default 100k chars/day). The frontend `ShopperTranslationProvider` (`frontend/src/storefront/src/context/ShopperTranslationContext.jsx`) **blocks first paint behind a small spinner** until the bundle resolves — shoppers never see English flash before Hindi, and there is NO Map / NO sessionStorage / NO localStorage of translations on the browser side (CDN edge is the only cache). Safety net: a 5s timeout in the provider unblocks render with empty dict if SiteContext never resolves a `siteId` so the storefront can never get stuck behind a permanent loader. The legacy `POST /api/storefront/:siteId/translate` proxy in `translate-worker.js` is still wired and the `/translate/purge` endpoint still works (merchant cache-purge UI calls it), but the storefront client itself no longer hits it on any code path.
  - **Single source of truth.** Both paths share `backend/utils/server-translator.js` for cache reads/writes, MS calls, daily-cap enforcement, and usage tracking — fixing a bug in the translator pipeline now fixes both shoppers' API content and shoppers' static UI labels in one place. Full design + phased rollout history in `docs/SHOPPER_TRANSLATION_REFACTOR.md`.
- **Active language is shopper-controlled via `localStorage`.** `ShopperTranslationContext` reads `localStorage.flomerce_lang`. The shopper-facing `LanguageSwitcher` (`frontend/src/storefront/src/components/LanguageSwitcher.jsx`) writes to that key and dispatches a `flomerce_lang_change` `CustomEvent`; the context listens for that event so every `<TranslatedText>` re-renders without a page reload. Cross-tab sync also works via the standard `storage` event.
- **Caching + spend caps.** The translate worker writes per-site translations to the `translation_cache` shard table (keyed by site_id + text_hash + target_lang) and tracks character usage per (site_id, year_month) in `site_translator_usage`. A separate cross-site `translation_memory` table (migration `0021_translation_memory.sql`, used by `backend/utils/translator.js`) acts as a translator memory for the merchant-credentials helper. The worker enforces a per-site daily character cap (`TRANSLATOR_DAILY_CAP`, default 100k chars/day), plus per-request limits (max 200 strings, 50k chars). Merchant admin → Shopper Language section can purge a site's cache after fixing a bad translation. The `i18n_overrides` table from migration `0020_i18n_overrides.sql` exists but is no longer read by any active code path.
- **Merchant configures translator in admin UI.** `ShopperLanguageSection.jsx` (under Settings) lets the merchant paste their MS Translator key/region, pick which languages to expose in the storefront switcher, test creds (`POST /api/sites/:id/translator-settings/test`), save (`PUT /api/sites/:id/translator-settings`), and clear the cache.
- **Still removed (do not restore without an explicit user request):** the wizard's "Default Language for Your Website" step; the platform `LanguageSwitcher` from any page outside Navbar; the storefront SPA's i18next packages; `useTranslation()` calls on any auth/legal/dashboard/wizard/owner-other-tab page.
- **Kept (intentional):** `sites.content_language` DB column — unused by app code now, retained as a reverse-migration safety hatch. `getTranslatorCredentials(env)` + `platform_settings.translator_api_key` in `admin-worker.js` — stale wiring for the now-deleted admin-UI translator; harmless and not in any active code path. Comment-only references to `i18n-worker.js` in `backend/utils/translator.js` and `backend/migrations/0020_i18n_overrides.sql` — leave them, do not "clean up".
- **Source catalog is exactly 4 JSON files** under `frontend/src/shared/i18n/locales/en/`: `landing.json`, `nav.json`, `common.json`, `languages.json`. The 11 other namespace JSONs that used to live there (auth/admin/owner/dashboard/products/customers/wizard/legal/about/plans/storefront) were deleted on Apr 24, 2026 because no UI surface calls `t()` for any of their keys anymore. If you ever wire up `useTranslation()` on a new page, recreate the matching JSON, add an `import` for it in both `frontend/src/shared/i18n/locales/en/index.js` (front end barrel) AND `backend/workers/platform/i18n-worker.js` (EN_CATALOG), then rebuild — non-English locales will lazy-regenerate to pick up the new namespace on next request.
- **Wizard SEO seed data (NOT a translation namespace).** Site-creation wizard step 3 pre-fills SEO title, SEO description, and starter category names based on the merchant's chosen business category (jewellery / clothing / beauty / general). Those English templates used to live inside `wizard.json` but were extracted to `backend/workers/platform/wizard-seed-data.js` when wizard.json was deleted. The merchant edits these freely during the wizard, so they don't need to be translated server-side — `<TranslatedText>` translates the merchant's final values on the storefront per shopper.
- **Adding a new English string:**
  - On the **landing page / Navbar / LandingPricing / ContactForm / AboutPage** (System A surfaces — the 5 platform component/page files; the shared `LanguageSwitcher` infrastructure under `frontend/src/shared/i18n/` also calls `useTranslation` but is not counted as a "surface" because it is generic chrome only mounted by `Navbar`): add the key + English value to the appropriate file under `frontend/src/shared/i18n/locales/en/` (`landing.json`, `nav.json`, `common.json`, `languages.json`) and reference it via `t('keyName')` in the JSX. Then rebuild the platform SPA. Non-English locales pick the new string up automatically the next time the worker regenerates them from the updated EN_CATALOG fingerprint.
  - On **every other platform page** (auth, dashboard, wizard, owner admin including the Translations tab, legal pages — terms / privacy / refund / shipping) and on the **storefront SPA** chrome: just write the literal English in the JSX. There is no key/namespace step. Shoppers viewing the storefront in a non-English language pick up new strings the next time `<TranslatedText>` renders them (cache miss → translate-worker → cache).
  - **Footer (landing + About) — what translates and what stays English.** *Translates:* the column headers (`footerCompany`, `footerContact`), `rightsReserved`, the tagline (`footerTagline`), the five page-link names (`footerAbout` / `footerTerms` / `footerPrivacy` / `footerRefund` / `footerShipping`), and the address line (`footerAddress` = "Karwar, Karnataka, India — 581400"). *Stays raw English:* brand `Flomerce`, phone `+91 9901954610`, email `SUPPORT_EMAIL`, and the contact-card address on AboutPage (the one labelled with `about.addressLabel` — separate from the footer). Phone and email are universal contact strings so they're left literal even in non-English locales. **Known UX trade-off:** clicking a translated footer link to one of the four legal pages (Terms / Privacy / Refund / Shipping) lands the user on a page whose `<h1>` and body are still in English — the legal page bodies are deliberately English-only (only their nav `LanguageSwitcher` and `LegalBindingBanner` translate). About is the one footer link that goes to a fully-translated destination. Owner explicitly chose this trade-off on Apr 24, 2026 after a brief period of keeping the link names raw English to avoid the mismatch; do not "fix" it back without an explicit user request.
- **DEPLOY GOTCHA — editing a System A translation JSON requires BOTH a frontend AND a backend deploy.** The English source files under `frontend/src/shared/i18n/locales/en/` (`common.json`, `nav.json`, `landing.json`, `languages.json`) live in the frontend tree, but `backend/workers/platform/i18n-worker.js` imports them statically and bundles them into the worker at build time as `EN_CATALOG`. Three things follow:
  1. **The frontend deploy alone does nothing useful.** It only ships the new `t('keyName')` references. If the worker isn't redeployed, `EN_CATALOG` still has the OLD keys → `diffStaleness()` reports every cached non-English locale as `upToDate: true` → the **"Refresh all out-of-date" button never appears** in the admin Translations panel, and clicking per-language "Regenerate" produces a Hindi catalog that's missing the new keys (so the SPA falls back to the English literal even after Regenerate).
  2. **The worker MUST be redeployed for the new EN catalog to ship.** `cd backend && npx wrangler deploy`. After a successful backend deploy, verify with `curl https://flomerce.com/api/i18n/locale/en` (incognito) and confirm the new key is present. Only then will the admin staleness diff light up and Regenerate / Refresh-all produce a Hindi catalog containing the new keys.
  3. **CI pipelines that only watch `backend/**/*.js` paths will skip the redeploy** when the only edit is to a `.json` file under `frontend/src/shared/i18n/locales/en/`, because no `.js` file under `backend/` actually changed (the JSON import is resolved at wrangler bundle time, not at git-diff time). If you hit this in the future, the cheapest workaround is to make a trivial edit to `backend/workers/platform/i18n-worker.js` (e.g. tweak a comment) in the same commit so CI sees a `.js` change. The cleaner fix is to broaden the CI path filter to also trigger backend deploys when `frontend/src/shared/i18n/locales/en/**/*.json` changes — pick whichever your pipeline supports.
  4. **End-to-end staleness chain after a successful backend deploy:** new EN bundled → worker's `EN_CATALOG` fingerprint differs from each cached locale's stored fingerprint → admin Translations panel shows orange "X translations out of date" banner with "Refresh all out-of-date" button → click it → worker re-translates only the changed keys via Microsoft Translator → writes new JSON to R2 → calls `purgeLocaleCache()` to bust the 7-day Cloudflare edge cache (requires `CF_API_TOKEN` wrangler secret + `CF_ZONE_ID` env var, both already configured) → next browser request fetches fresh catalog from origin → SPA hard-refresh required to drop i18next's in-memory resource store → user sees translated text. **Every step must succeed; the chain is fragile precisely because each layer caches aggressively.** Standalone "translate and ship" workflows are intentionally avoided — the staleness diff is the source of truth and humans gate the spend.

### Database Tables
- **Platform DB:** `users`, `sessions`, `sites`, `subscriptions`, `payments`, `shards`, `enterprise_sites`, `enterprise_usage_monthly`.
- **Shard DBs:** `site_config`, `categories`, `products`, `orders`, `carts`, `site_customers`, `reviews`, `page_views`, `site_media`. All tables include `row_size_bytes` and are filtered by `site_id`.

### Technology Stack
- **Frontend:** React 19 + Vite, Cloudflare Pages.
- **Backend:** Cloudflare Workers.
- **Database:** Cloudflare D1.
- **File Storage:** Cloudflare R2.
- **Authentication:** Custom JWT for platform users, verification-code for site admins, and custom customer auth for storefronts, including Google Sign-In.
- **UI/UX:** Theme-based template system with extensive admin controls. Classic (default) and Modern themes share one React SPA; theme-specific component variants live in `frontend/src/storefront/src/components/templates/<theme>/`. ThemeContext reads `settings.theme` from site config to select variants. Shared sections (WatchAndBuy, FeaturedVideo, ShopTheLook, etc.) work across all themes unchanged. Modern theme uses separate component files (not CSS overrides on shared components): `NavbarModern`, `FooterModern`, `ProductCardModern`, `HeroSplit`, `CategoryGrid`, `CheckoutPageModern`, `ReviewPageModern`, `ProductReviewsModern`, `CustomerReviewsModern`, `TrendingNow`, `BrandStory`, `ChooseByCategoryModern`. Parent components use `const Active = isModern ? ModernVariant : ClassicVariant` pattern for switching. Modern CSS uses `mn-` prefixed classes (self-contained per component) in `modern.css`. Design: Inter font, `#111` accent, sharp corners (borderRadius: 0), flat/no shadows. Modern homepage replaces Classic sections (WatchAndBuy, FeaturedVideo, ShopTheLook) with `TrendingNow` (horizontal scrollable product row), `BrandStory` (split image/text brand narrative), and `ChooseByCategoryModern` (edge-to-edge grid with hover reveals). Some pages (CategoryPage, ProductDetailPage, etc.) still use `.modern-theme` CSS class wrapper with descendant selectors in `modern.css` for their overrides.
- **SEO:** Server-side meta tag injection, dynamic sitemap.xml, robots.txt per tenant, and structured data for products, articles, and reviews. Default SEO titles and meta descriptions are category-aware and generated.
- **CDN Edge Caching:** Two-tier caching strategy. Browser: `max-age=60` (60s). CDN: `CDN-Cache-Control: max-age=604800` (7 days) with stale-while-revalidate. Admin requests bypass browser cache entirely (`cache: 'no-store'` in `api.js` and `SiteContext.jsx`). Write operations trigger `purgeStorefrontCache()` which purges both Workers Cache API and Cloudflare CDN edge globally (via Cloudflare API using `CF_API_TOKEN` + `CF_ZONE_ID`). Purge covers both `siteId` and `subdomain` URL variants. Cached endpoints: `/api/site`, `/api/products`, `/api/categories`, `/api/blog/posts`, `/api/blog/post/:slug`, `/api/reviews/product/:id`. Purges are debounced per-site by default: the first call for a `siteId` opens a fixed 30s window (configurable via `env.PURGE_DEBOUNCE_MS`), and every subsequent purge call within that window for the same site is coalesced — its `types` and `resourceIds` are merged into the same pending entry and a single Cloudflare API call fires when the window expires. The window is NOT reset by later edits (bounds `ctx.waitUntil` to ≤ one window). Coalesce state is in-memory per Worker isolate, so concurrent admin requests on different isolates each run their own debouncer (worst case: one purge per isolate per window per site — still dramatically better than per-edit). Pass `{ immediate: true }` as the 5th arg to `purgeStorefrontCache(env, siteId, types, resourceIds, { immediate: true })` to bypass debouncing for events that must purge instantly (site deletion, manual admin purge action). Stale risk on isolate eviction during the 30s window: at most one merchant edit's purge is dropped; affected URLs stay stale until next edit triggers a purge or until the 7-day TTL expires.
- **Push Notifications:** Full Web Push Protocol (VAPID) implementation using Cloudflare Workers, with service worker for client-side handling and admin panel for management and auto-triggers.
- **Store Logo:** Configurable logo upload, sizing, and positioning in the admin panel.
- **Platform Landing Page:** Full landing page with hero, features, pricing, contact, and footer sections.
- **Legal/Policy Pages:** Standard legal pages (T&C, Privacy, Refund, Shipping) with Razorpay compliance.
- **Terms Agreement:** Mandatory agreement to terms during signup and site creation.

### Key Features
- **Dynamic Content:** Configurable homepage elements and centralized default content per business category.
- **Template/Theme System:** Theme infrastructure with Classic (serif/gold/slider) and Modern (sans-serif/minimal/grid) variants. Config: `frontend/src/storefront/src/config/themes.js`. Context: `frontend/src/storefront/src/context/ThemeContext.jsx`. Modern components: `frontend/src/storefront/src/components/templates/modern/`. Theme is stored in `site_config.settings.theme` and selected during site creation. `useTheme()` hook returns `{ ...themeConfig, isModern }` for easy conditional rendering. ALL storefront pages now have Modern variants. Separate Modern component files (`*Modern.jsx`) are used for: Navbar, Footer, ProductCard, Hero, CategoryGrid, CheckoutPage, ReviewPage, ProductReviews, CustomerReviews. Parents switch via `const Active = isModern ? ModernVariant : ClassicVariant`. Remaining pages (CategoryPage, ProductDetailPage, etc.) use `.modern-theme` CSS class wrapper + descendant selectors in `modern.css`. Modern CSS classes use `mn-` prefix (self-contained per component).
- **Admin Panel:** Centralized site content, policy, and SEO management with iframe preview and "Shop the Look" tab. Edit Website tabs are template-aware: shared sections (Navbar, Hero, Categories, etc.) appear for all themes, while template-specific sections (Classic: Watch & Buy, Featured Video, Shop the Look, Store Locations; Modern: Trending Now, Brand Story) only appear when the matching theme is active. **Visual Customizer** (`VisualCustomizer.jsx`): Full-screen preview-first editing experience accessible from the "Edit Website" page on desktop. Features: large live iframe preview, slim left sidebar with Sections/Pages/Settings tabs, section show/hide toggles (eye icon), drag-and-drop section reordering, desktop/mobile device preview toggle, click-to-edit section editors in a sliding panel. Sections visibility changes auto-save to backend. Launched via the blue "Open" button banner at the top of the classic editor; the classic tab-based editor remains available. PromoBannerEditor was extracted to its own file (`PromoBannerEditor.jsx`).
- **Payment-First Site Creation:** When a non-trial user creates a site, the site is only created in the database AFTER payment is confirmed. The wizard collects all data, passes it to the PlanSelector, which creates a Razorpay subscription without a siteId. Only after payment verification succeeds does the frontend call `onCreateSite()`. The backend auto-links unlinked active subscriptions to newly created sites.
- **Duplicate Subscription Prevention:** Unique index on `subscriptions.razorpay_subscription_id` prevents duplicate records. `activateSubscription()` uses `INSERT OR IGNORE` (atomic) and reconciles existing rows on conflict. Webhook events are deduped through the `processed_webhooks(event_id PK)` table — `handleRazorpayWebhook` inserts the event id (header `x-razorpay-event-id` / `payload.id`) once and returns early if already seen. `handleSubscriptionCharged` also skips no-op renewals when `current_period_end` is unchanged.
- **Plan Downgrade Flow (scheduled, not immediate):** When a user picks a lower-tier plan for a site that already has an active higher-tier subscription, `createRazorpaySubscription` keeps the current plan running until its `current_period_end`. It creates the new Razorpay subscription with `start_at = current_period_end` (epoch seconds) and asks Razorpay to cancel the higher plan at cycle end (`cancel_at_cycle_end: 1`). The new sub is stored locally with `status='scheduled'` (via `activateSubscription`, which detects future `start_at`). `sites.subscription_plan` does NOT change yet, so paid features remain available until the cycle ends. The `getUserSites` API returns `subscription.scheduledPlan / scheduledStartAt` for the dashboard. To prevent stacking, a second downgrade attempt while one is already scheduled returns `409 PLAN_CHANGE_PENDING`. The cron (`cleanupExpiredData`) and the `subscription.cancelled` webhook both promote due-scheduled subs to `active` and update the cached `sites.subscription_plan`. Upgrades and same-tier swaps still take effect immediately.
- **Trial & Subscription Cleanup (cron):** `cleanupExpiredData` runs on the scheduled handler and: marks `active` subs whose `current_period_end < now` as `expired`; activates due `scheduled` subs (downgrades) and updates `sites.subscription_plan`; sets `is_active = 0` on trial sites whose `subscription_expires_at` has passed; clears the cached `sites.subscription_plan` when no active sub exists; deletes orphan `pending_subscriptions` older than 24h; trims `processed_webhooks` rows older than 30 days. `getUserSites` no longer trusts a stale `sites.subscription_plan` when the `subscriptions` table has no active row.
- **Subscription Cancel Webhook:** With `cancel_at_cycle_end: 1`, Razorpay fires `subscription.cancelled` after the cycle ends naturally. The handler now marks the row cancelled, clears `sites.subscription_plan`, sets `subscription_expires_at` to the actual period end (not `now`), and immediately promotes any waiting `scheduled` sub for the site.
- **Order-Before-Payment:** Storefront checkout creates the order with `pending_payment` status before opening Razorpay. Payment verification updates the existing order to `paid`. If payment fails, the stale order is auto-cancelled after 30 minutes by the scheduled cleanup.
- **Stock Reservation:** Stock is decremented at order creation (not after payment), preventing overselling. Stock is restored when orders are cancelled. Stale `pending_payment` orders older than 30 minutes are auto-cancelled with stock restored.
- **Customer Session Optimization:** `validateCustomerAuth` first checks `siteId` from URL params for direct DB lookup, falling back to shard scan only when needed.
- **Shop the Look:** Interactive product showcase on the homepage with clickable dots linked to products.
- **Product Options:** Supports Color, Custom Selection, and Priced options.
- **Subcategories:** Three-level category hierarchy with associated homepage sections.
- **Multi-Currency Support:** Product prices stored in default currency, with frontend conversion using `exchangerate-api.com`.
- **Additional Notification Recipients:** Owners can add up to 5 staff/team email addresses (Settings → Contact → "Additional notification emails") to receive the same owner-bound order notifications (new order, cancellation request, return request, order cancelled, order delivered). Stored in `site_config.settings.notificationCcEmails` (array). Helper `getOwnerRecipients(settings, config)` in `backend/utils/email.js` returns the deduped, validated list (primary owner email + CC emails). Gated as a Growth+ feature in the admin UI; backend sanitizes/caps to 5 valid emails. Customer-facing emails are unaffected.
- **WhatsApp Business Notifications:** Automated order updates via WhatsApp Business API (Meta Cloud API or Interakt). Store owners bring their own API credentials, stored in `sites.settings` JSON. Supports template and plain text messages. Order notifications: confirmed, packed, shipped (with tracking), delivered (with review link), cancelled. Customers opt-in at checkout. Backend: `backend/utils/whatsapp.js`. Schema: `whatsapp_opted_in` column on `orders` and `guest_orders`. Gated as a Growth+ feature (credential-based).
- **Order Flow:** Full order lifecycle (Pending to Delivered) with COD and Razorpay, stock management, customer notifications, and order tracking.
- **Order Cancellation:** Opt-in customer-initiated order cancellation with admin approval workflow.
- **Return Orders:** Opt-in customer-initiated return system with admin approval/refund workflow, supporting photos and resolution preferences.
- **Order Help Page:** Unified page for order-related actions (Track, Cancel, Return, Contact).
- **Product Reviews:** Per-product customer review system with star ratings, optional images, and admin moderation.
- **Inventory Locations:** Multi-location inventory tracking with stock transfers and fulfillment priority.
- **Delivery Charges:** Configurable global shipping fees with flat rates, free-above thresholds, and country/region-based overrides. Checkout address forms support international countries with dynamic state dropdowns.
- **International Phone Input:** Phone fields use a country code dropdown with flag emoji + dial code (e.g., 🇮🇳 +91) and searchable country selector. Auto-selects dial code based on selected country. Phone stored in E.164 format (e.g., `+919876543210`). Shared `PhoneInput` component at `frontend/src/storefront/src/components/ui/PhoneInput.jsx`. Country dial codes in `countryStates.js` (`COUNTRIES[].dial`, `COUNTRY_FLAGS`, `getDialCode()`). Phone field is also included in customer signup form (optional).
- **Shiprocket Shipping Integration (Apr 25, 2026):** Per-tenant opt-in integration for India merchants. Merchants connect via Shiprocket email/password in admin Settings → Shipping (Shiprocket); credentials are encrypted at rest (AES-GCM via `crypto.js#encryptSecret`) and never returned to the client. The integration auto-handles login → token refresh (10-day TTL, persisted via `json_set('$.shiprocket._tokenCache')` to avoid clobbering concurrent setting writes), order create, AWB assignment, pickup schedule, and label generation. **Routes:** all admin endpoints under `/api/shipping/*` (status, connect, disconnect, settings, pickup-locations, orders/:id/{ship,cancel,label,track}); webhook at `/api/webhooks/shiprocket/:siteId` authenticated by per-tenant `X-Api-Key` token (auto-generated on connect, displayed in admin UI for merchants to paste into the Shiprocket dashboard). Webhook is idempotent via `shiprocket_last_event_at` comparison and updates the order's status; when status flips to `shipped`/`delivered` the existing `updateOrderStatus` flow fires (emails/WhatsApp). **Auto-ship hooks:** `orders-worker.createOrder` for COD orders (via `ctx.waitUntil`) and `payments-worker.processPostPaymentActions` for paid orders (also via `ctx.waitUntil` — threaded through `handlePayments → verifyPayment → processPostPaymentActions(env, order, ctx)` so a slow Shiprocket call never blocks the customer's payment confirmation response). Both hooks gated by per-merchant toggles `autoShipOnConfirmCod` / `autoShipOnPayment`. **Resilience:** `shipOrderViaShiprocket` resumes from prior partial state — if `shiprocket_order_id`/`shiprocket_shipment_id` exist but AWB assign previously failed, it skips the create-order step to avoid duplicate Shiprocket orders. **Manual override:** OrdersSection modal exposes Ship Now / Reprint Label / Track / Cancel Shipment buttons gated on `siteConfig.settings.shiprocketEnabled` (the only Shiprocket field exposed in public site-info — full creds object stripped in `index.js#publicSettings`). Schema columns added to both `orders` and `guest_orders`: `shiprocket_order_id`, `shiprocket_shipment_id`, `shiprocket_awb`, `shiprocket_courier`, `shiprocket_label_url`, `shiprocket_status`, `shiprocket_last_event_at`, `shiprocket_error`. Files: `backend/utils/shiprocket.js` (HTTP wrapper), `backend/workers/storefront/shipping-worker.js` (admin endpoints + webhook + `shipOrderViaShiprocket` helper), `frontend/src/storefront/src/components/admin/SettingsSection.jsx` (Shipping card), `frontend/src/storefront/src/components/admin/OrdersSection.jsx` (action buttons). India-only (`country: 'India'`); manual order flow remains fully functional when integration is off.
- **Shiprocket Phase 1 — Per-Product Weight (Apr 26, 2026):** Foundation for accurate courier rate / ETA calculations. Adds a `Shipping Details` card to the admin product form (`ProductForm.jsx`) with required `Weight (g)` and optional `Length / Breadth / Height (cm)` inputs, conditionally rendered when `siteConfig.settings.shiprocket.enabled`. Server-side validation in `products-worker.js` (`createProduct` always; `updateProduct` only when `weight` is in the payload) — gating helper `siteHasShiprocketEnabled()` is fail-closed (errors propagate to outer 500 handler). Order placement (`orders-worker.js`, both auth and guest paths) now snapshots `weight` into the per-item JSON and hard-errors with `CART_ITEM_MISSING_WEIGHT` (listing the offending product names) when Shiprocket is on and any cart item has no weight. Also adds positive-integer quantity validation in both order paths. `shipping-worker.js` introduces `sumItemsWeightKgStrict(items)` (uses `??` for quantity to reject `0`, requires every item to have weight > 0) — `shipOrderViaShiprocket` calls it pre-build and returns code `NO_ITEM_WEIGHT` if any line item is missing weight, never falling back to the legacy `defaultWeight`. `buildShiprocketOrderPayload` now accepts a 5th `totalWeightKg` parameter (only caller is `shipOrderViaShiprocket`). Phases 2–4 (serviceability + customer ETA on cart/checkout, default-courier setting + admin courier picker modal + fallback chain, dynamic per-courier pricing + markup gated on auto-courier) are pending.
- **Abandoned Cart Reminders:** Automated reminders via WhatsApp and/or email for logged-in customers who leave items in their cart. Settings in admin panel under "Abandoned Cart Reminders": enable/disable, delay hours (1/3/6/12/24h), max reminders (1-3), channel selection (email/WhatsApp). Processed by Cloudflare Cron Trigger (`scheduled` handler in `index.js`). Cart table has `reminder_sent_at` and `reminder_count` columns — reset when cart is updated. Subsequent reminders use exponential backoff (delay doubles each time). Skips carts where the customer has placed an order since the cart was last updated. WhatsApp template: `abandoned_cart_reminder`. Email template: `buildAbandonedCartEmail()` in `email.js`. WhatsApp builder: `buildAbandonedCartWA()` in `whatsapp.js`. Config stored in `site_config.settings.abandonedCartConfig`.

### Shared UI Components (Alerts / Confirms / Toasts)
- **Single source of truth:** `frontend/src/shared/ui/` (imported by both the storefront and platform apps).
  - `Modal.jsx` — base overlay/box primitive (Esc to close, scroll lock, backdrop click).
  - `AlertModal.jsx` — declarative alert dialog with variants (`info`, `success`, `warning`, `error`, `upgrade`); supports `primaryAction`/`secondaryAction` (button or link). Also exports `isPlanError(err)`.
  - `ConfirmDialog.jsx` — `<ConfirmProvider>` + `useConfirm()` promise-based hook: `const ok = await confirm({ title, message, confirmText, cancelText, variant: 'danger' })`.
  - `Toast.jsx` — `<ToastProvider>` + `useToast()` with `.success/.error/.warning/.info(msg, { title?, duration? })`.
  - `styles.css` — all styling (BEM-style `flo-ui-*` classes, mobile-responsive, font-awesome icons).
  - `index.js` — barrel re-exports.
- **Providers** are mounted at the top of both `App.jsx` files (`<ToastProvider><ConfirmProvider>...`).
- **Plan-limit/upgrade prompts** use the shared `AlertModal` directly with `variant="upgrade"` (the old `PlanLimitModal.jsx` wrapper files have been removed). Callers: `platform/src/pages/DashboardPage.jsx` and `storefront/src/components/admin/LocationsSection.jsx`.
- **Storefront vite config** has a `resolve.alias` block pointing `react`, `react-dom`, and the JSX runtimes back to its own `node_modules/` so files in `frontend/src/shared/` resolve React correctly.
- **Migration goal:** progressively replace remaining `window.alert(...)` / `window.confirm(...)` / one-off inline modals with `useToast()` / `useConfirm()` / `<AlertModal>`.

### Production vs Local Development
- **Always plan and reason for production first.** This codebase runs on Cloudflare Pages + Workers + D1 + R2. The local Wrangler dev mode is a Miniflare simulation and does NOT replicate production behavior for: edge cache (Cloudflare CDN cache for public endpoints), custom domain SSL provisioning, scheduled (cron) triggers, D1 sharding routing in production, R2 region pinning, and rate limits.
- **Code or DB changes don't reach the live site until both the backend Worker and frontend Pages are redeployed.** Local restarts only affect the Miniflare simulation.
- **For production debugging,** use `fetch_deployment_logs` (Worker logs) rather than relying on local console output.

## External Dependencies
- **Cloudflare Pages:** Frontend hosting.
- **Cloudflare Workers:** Backend serverless compute.
- **Cloudflare D1:** Primary database.
- **Cloudflare R2:** Object storage.
- **Cloudflare REST API:** D1 database management and worker configuration.
- **Razorpay:** Payment gateway.
- **Brevo:** Transactional email service (`https://api.brevo.com/v3/smtp/email`).
- **WhatsApp Business API:** Meta Cloud API (`graph.facebook.com/v21.0`) or Interakt (`api.interakt.ai/v1`) for order notifications. Per-store credentials.
- **api.exchangerate-api.com:** Exchange rate data for multi-currency support.